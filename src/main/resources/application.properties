# Application Name
spring.application.name=taskmaster

# Server Port
server.port=8080

# MySQL Database Configuration (Local)
spring.datasource.url=jdbc:mysql://localhost:3306/taskmaster_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=M7548872673

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.devops.taskmaster=DEBUG